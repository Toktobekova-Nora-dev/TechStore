import React from "react";
import scss from "./FirestSession.module.scss";

const FirestSession = () => {
  return (
    <div className={scss.FirestSession}>
      <div className="container">
        <div className={scss.FirestSession}>FirestSession</div>
      </div>
    </div>
  );
};

export default FirestSession;
